generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["interactiveTransactions"]
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model A {
  id String @id @default(dbgenerated("(bin_to_uuid(uuid_to_bin(uuid(), 1), 0))")) @db.VarChar(36)
}
