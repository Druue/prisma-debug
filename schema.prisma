generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model B {
  id    Int         @id @default(autoincrement())
  some  Enumeration
  other OtherEnum
}

enum Enumeration {
  field
  newField
}

enum OtherEnum {
  otherField
}
